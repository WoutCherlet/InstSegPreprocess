import os
import sys

import open3d as o3d
import numpy as np





def overlap_distance(tiles, trees):

    # TODO: similar here as for robsoncreek, overlap the segmented trees back onto the original tiles and remove the points
    # doing this because in the saved understory tiles, a small area around the trunk base has been removed
    # we do keep the original saved trees as reference, but just regenerate the understory tiles


    return





def main():
    
    TILES = ""
    TREES = ""


    overlap_distance()

    return

if __name__ == "__main__":
    main()